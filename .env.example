PORT=8000
HOST=0.0.0.0
HTTPS_PORT=5443
JWT_SECRET=supersecretjwt
JWT_EXPIRES_IN=7d

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=calendar_app
# Alternatively provide a DATABASE_URL like: postgres://user:pass@host:5432/dbname
DATABASE_URL=

# Email
EMAIL_FROM=no-reply@example.com

# Cron / jobs
# Adjust reminder window by editing reminder.job.js

# Optional HTTPS (provide absolute paths to cert & key if you enable)
# SSL_CERT_PATH=./certs/server.crt
# SSL_KEY_PATH=./certs/server.key

# When using ngrok, keep HOST=0.0.0.0 and just run: ngrok http 8000
